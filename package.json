{
  "name": "sharepass",
  "version": "1.0.0",
  "private": true,
  "description": "Emotional support system app",
  "engines": {
    "node": ">=18.17.0"
  },
  "scripts": {
    "clean": "node scripts/clean-next.mjs",
    "clean:dev": "node scripts/clean-next.mjs .next-dev",
    "clean:build": "node scripts/clean-next.mjs .next",
    "predev": "npm run clean:dev",
    "prebuild": "npm run clean:build",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "mongodb": "^7.2.0",
    "next": "^14.0.0",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-next": "^14.0.0"
  }
}
